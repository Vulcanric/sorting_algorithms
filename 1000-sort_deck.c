#include "sort.h"

/**
 * sort_deck - sorts a deck of cards.
 * @deck: A pointer to the deck pointer
 */
void sort_deck(deck_node_t **deck)
{
	if (deck == NULL || *deck == NULL || (*deck)->next == NULL)
		return;
}
